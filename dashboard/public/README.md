# Static assets

Place **seed.png** in this folder (`dashboard/public/`).

It is used as:
- **Favicon** (browser tab icon)
- **Logo** in the sidebar (next to "Leadseed")
- **Logo** on the login page

The file is served at `/seed.png`. Use a square image (e.g. 64×64 or 128×128 px) for best results.
